fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
psycopg2-binary>=2.9.9
PyMySQL>=1.1.0
google-cloud-pubsub>=2.21.0
google-cloud-firestore>=2.16.0
# MVP P0 backends.
hvac>=2.1.0          # HashiCorp Vault client (transit KMS + KV secrets for all 3 providers)
nats-py>=2.7.0       # NATS client (eventing broker for EventBridge + Eventarc + Event Grid)
boto3>=1.34.0        # AWS SigV4 → MinIO/DynamoDB/ElasticMQ; also AWS conformance harness
cedarpy>=4.8.0       # Cedar policy engine (real AWS IAM eval; reused for GCP IAM + Azure RBAC)
