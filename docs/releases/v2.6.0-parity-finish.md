# Vyomi v2.6.0 — Parity Finish

**Goal:** close the *rest* of `docs/PARITY-GAPS.md` after v2.5.0 — the remaining
operation-depth (P2), cross-cloud symmetry (P3), the net-new data planes (P4), and the
live-switchover edge (P1 #6).

**Principles (unchanged):** additive; the same substrate-free conformance cores (so Nano
gains every op too); each op proven by a conformance test; backed stores stay Pro/Max-only.

Status: IN PROGRESS · Target: v2.6.0 · Created 2026-07-13

---

## Phases

### Phase A — Messaging depth  (PARITY P2 #9)  ✅ DONE
- [x] SNS **filter policies** — Set/GetSubscriptionAttributes + per-subscription delivery filtering (exact + numeric + prefix + anything-but + exists; AND-across-keys); message attributes off the flattened Query wire ✅
- [x] SQS **FIFO** (.fifo: MessageGroupId required, content-based dedup, per-group ordering/locking) + **DLQ** redrive (RedrivePolicy maxReceiveCount → dead-letter target) ✅
- [x] tests: test_sqs_fifo_dlq_core.py, test_sns_filter_policy_core.py; messaging + router + matrix stay green ✅
- [x] commit ✅

### Phase B — NoSQL / document depth  (PARITY P2 #8)
- [ ] DynamoDB **GSI** (global secondary index query)
- [ ] Firestore **richer queries** (structured query: where >,<,IN + orderBy)
- [ ] commit

### Phase C — Crypto depth  (PARITY P2 #10)
- [ ] Azure KV keys **wrap/unwrap** + **sign/verify** (+ additional algorithms)
- [ ] KMS / Secrets **rotation** (all clouds) — P2 #9 rotation
- [ ] commit

### Phase D — Object depth  (PARITY P2 #7)
- [ ] S3 **multipart upload** (Create/UploadPart/Complete/Abort)
- [ ] GCS **resumable upload**
- [ ] commit

### Phase E — Azure symmetry  (PARITY P3 #12/#13)
- [ ] Azure **SQL data plane** (Postgres-compat on the SqlStore seam — the RDS/Cloud SQL peer)
- [ ] Azure **IAM/RBAC decision core** (role-assignment → checkAccess evaluator)
- [ ] commit

### Phase F — Net-new data planes  (PARITY P4 #14/#15)
- [ ] Event bus data plane (EventBridge / Event Grid subscriptions + delivery) — closest to messaging
- [ ] Serverless invoke (Lambda/Functions/Function App — synchronous invoke of stored code) — scoped
- [ ] API gateway / VPC — scoped (catalog today; assess a minimal data plane)
- [ ] commit(s)

### Phase G — Live switchover + close-out  (PARITY P1 #6)
- [ ] DynamoDB + Azure-Blob live-switchover edges (tenant gate / container edge) — needs running appliance
- [ ] full sweep, VERSION → 2.6.0, CHANGELOG, PARITY-GAPS update, tag v2.6.0

---

## Progress log

_(dated entries appended as phases complete)_
