# Vyomi v2.6.0 — Parity Finish

**Goal:** close the *rest* of `docs/PARITY-GAPS.md` after v2.5.0 — the remaining
operation-depth (P2), cross-cloud symmetry (P3), the net-new data planes (P4), and the
live-switchover edge (P1 #6).

**Principles (unchanged):** additive; the same substrate-free conformance cores (so Nano
gains every op too); each op proven by a conformance test; backed stores stay Pro/Max-only.

Status: ✅ COMPLETE · Shipped: v2.6.0 · Created 2026-07-13

---

## Phases

### Phase A — Messaging depth  (PARITY P2 #9)  ✅ DONE
- [x] SNS **filter policies** — Set/GetSubscriptionAttributes + per-subscription delivery filtering (exact + numeric + prefix + anything-but + exists; AND-across-keys); message attributes off the flattened Query wire ✅
- [x] SQS **FIFO** (.fifo: MessageGroupId required, content-based dedup, per-group ordering/locking) + **DLQ** redrive (RedrivePolicy maxReceiveCount → dead-letter target) ✅
- [x] tests: test_sqs_fifo_dlq_core.py, test_sns_filter_policy_core.py; messaging + router + matrix stay green ✅
- [x] commit ✅

### Phase B — NoSQL / document depth  (PARITY P2 #8)  ✅ DONE
- [x] DynamoDB **GSI** — CreateTable persists GSI KeySchema, DescribeTable emits it, Query(IndexName=…) resolves the index's own keys; unknown-index → ValidationException ✅
- [x] Firestore **richer queries** — full fieldFilter operators (>,<,>=,<=,!=,IN,NOT_IN,ARRAY_CONTAINS[_ANY]), unaryFilter (IS_NULL/IS_NOT_NULL), compositeFilter AND/OR, orderBy (+direction), offset/limit ✅
- [x] tests: test_dynamodb_gsi_core.py, test_firestore_query_core.py; base suites green ✅
- [x] commit ✅

### Phase C — Crypto depth  (PARITY P2 #10 + #9 rotation)  ✅ DONE
- [x] Azure KV keys **wrap/unwrap** (envelope a symmetric key) + **sign/verify** (deterministic MAC-based signature; self-consistent round-trip) ✅
- [x] AWS KMS **rotation** — EnableKeyRotation/DisableKeyRotation/GetKeyRotationStatus/RotateKeyOnDemand (ciphertext still decrypts after rotation) ✅
- [x] AWS Secrets Manager **RotateSecret** — new AWSCURRENT version, old demoted to AWSPREVIOUS ✅
- [x] tests: test_azure_kv_crypto_core.py, test_kms_secrets_rotation_core.py; base KMS/Secrets/KV suites green ✅
- [x] commit ✅
- NOTE: sign/verify uses a MAC-based signature (stdlib, Pyodide-safe), not native RSA/ECDSA — a self-consistent sign→verify pair that an unmodified KV client round-trips.

### Phase D — Object depth  (PARITY P2 #7)  ✅ DONE
- [x] S3 **multipart upload** — CreateMultipartUpload / UploadPart / ListParts / CompleteMultipartUpload (assembles parts, canonical `md5(concat part-md5s)-N` ETag) / AbortMultipartUpload ✅
- [x] GCS **resumable upload** — initiate (uploadType=resumable → session URI in Location) + finalize PUT (single-use session) ✅
- [x] tests: test_s3_multipart_core.py, test_gcs_resumable_core.py; base S3/GCS suites green ✅
- [x] commit ✅

### Phase E — Azure symmetry  (PARITY P3 #12/#13)  ✅ DONE
- [x] Azure **SQL data plane** — `azure_sql_core.py`: server/database management + REAL SQL (CREATE/INSERT/SELECT) on the SqlStore seam (sqlite in Nano, Postgres behind the seam in Pro/Max); sync + async execute ✅
- [x] Azure **RBAC decision core** — `azure_iam_core.py`: `checkAccess` evaluator (built-in Owner/Contributor/Reader + custom roles; wildcard actions, notActions, scope inheritance, explicit-deny-wins) ✅
- [x] tests: test_azure_sql_core.py, test_azure_rbac_core.py ✅
- [x] commit ✅
- NOTE: served via dedicated appliance endpoints, not the native-SDK relay router (Azure SQL's real wire is TDS / RBAC is ARM — neither is a native-SDK-over-relay surface, so no S3/ARM router collision).

### Phase F — Net-new data planes  (PARITY P4 #14/#15)  ✅ DONE (event bus + serverless; APIGW/VPC scoped)
- [x] **Event bus** data plane — `eventbridge_core.py`: rules + event patterns (exact/prefix/anything-but/exists, nested detail) + SQS targets; PutEvents matches → delivers to targets. Wired into the router (AWSEvents.* JSON wire, shares MessagingStore) ✅
- [x] **Serverless invoke** — `lambda_core.py`: full function lifecycle + synchronous Invoke that RUNS the handler in a **sandboxed Python runtime** (safe builtins, no __import__/open/eval; handler errors → X-Amz-Function-Error). Wired into the router (/2015-03-31/functions REST) ✅
- [x] tests: test_eventbridge_core.py, test_lambda_core.py; router routes both without collision ✅
- [x] commit ✅
- **API gateway / VPC — DEFERRED (honest scope, not a false green):** a faithful API-GW data plane (route→integration→backend invoke, authorizers, stages) and a VPC network-simulation (subnets/route-tables/SG packet-eval/reachability) are each a large subsystem; a shallow stub would read as "covered" when it isn't, violating the native-SDK-conformance north star. Tracked as PARITY P4 remainder for a dedicated release.

### Phase G — Live switchover + close-out  (PARITY P1 #6)  ✅ DONE
- [x] Live native-SDK sweep against the running unified ingress (:9200 → simulator :9000): **S3 + SQS green end-to-end on real backends** (create bucket/put/get; send/receive) ✅
- [x] **DynamoDB "403 edge" ROOT-CAUSED** — it is the **tier-licensing gate** (`x-vyomi-tier-denied: tier_service_locked`, "dynamodb (nosql) requires Max tier", active_tier=free), NOT an ingress defect. Working as designed; testing DynamoDB live needs a Max-tier license ✅
- [x] **Azure-Blob "404 edge" ROOT-CAUSED** — the *current* router code returns container→201 / blob→201 / get→200 (verified); the live 404 is the running container serving **pre-v2.5.0 code**, not a core defect ✅
- [x] full conformance sweep (41 host-green + 3 backed-store green in venv on real NATS/Postgres/MinIO), VERSION → 2.6.0, CHANGELOG, PARITY-GAPS update, tag v2.6.0 ✅
- **REMAINING (not a code fix):** a coordinated redeploy of the shared simulator container with current code would make the live Azure-Blob path green; not done unilaterally (shared container, parallel sessions). DynamoDB-live stays tier-gated by design.

---

## Progress log

- **2026-07-13 — Phases A–F ✅** All operation depth (P2), cross-cloud symmetry (P3) and the
  tractable net-new data planes (P4 serverless + event bus) shipped as 6 commits, each with
  conformance tests; every op added to the shared cores (Nano + Pro/Max). 12 new capabilities,
  6 new cores (azure_servicebus already in v2.5.0; +azure_sql, azure_iam, eventbridge, lambda),
  ~14 new conformance test files.

- **2026-07-13 — Phase G ✅ (v2.6.0 SHIPPED)** Live sweep vs the running unified ingress proved
  S3 + SQS green on real backends; root-caused the two "switchover edges" (DynamoDB = tier gate by
  design; Azure-Blob = stale container code, current code verified 201/200). Full conformance sweep
  green (41 host + 3 backed-store in venv). VERSION → 2.6.0, CHANGELOG, PARITY-GAPS updated
  (P2 + P3 fully closed; P4 serverless/event-bus closed, API-GW/VPC deferred), tag v2.6.0.
  **P2 and P3 are now 100% closed; the only remaining parity items are API-gateway + VPC data
  planes (deliberately deferred as dedicated-release-sized subsystems).**

_(dated entries appended as phases complete)_
