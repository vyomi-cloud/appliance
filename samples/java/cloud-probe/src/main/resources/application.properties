# cloud-probe service port (override with PROBE_PORT or --server.port)
server.port=${PROBE_PORT:8080}
spring.application.name=cloud-probe
# Keep startup quiet + fast; we're a probe, not a web app.
spring.main.banner-mode=off
logging.level.software.amazon.awssdk=WARN
